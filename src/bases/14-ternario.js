

let firstName;
let lastName = 'Herrera';


// console.log(`${ firstName || 'No firstName' } ${ lastName || 'No lastName' }`);

const isActive = false

const msj = isActive ? 'Esta activo' : 'No esta activo'

console.log(msj);